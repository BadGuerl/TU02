// Escribe una función pow(x, n) que devuelva x en la potencia n.O, en otras palabras, que multiplique x por sí mismo n veces y devuelva el resultado.
// pow(3, 2) = 3 * 3 = 9
// pow(3, 3) = 3 * 3 * 3 = 27
// pow(1, 100) = 1 * 1 * ...* 1 = 1
// Para comprobar la función, muestra en la consola de JavaScript, a nivel de información, la salida de pow(2, 6), la salida de pow(0, 5) y la salida de(-5, 3)

const potencia = Math.pow(2, 6);
const potencia2 = Math.pow(0, 5);
const potencia3 = Math.pow(-5, 3);


console.log(potencia);
console.log(potencia2);
console.log(potencia3);
